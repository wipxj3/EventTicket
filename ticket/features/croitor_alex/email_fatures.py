# -*- coding: utf-8 -*-
from lettuce import step

@step(u'Given I am a registered user')
def given_i_am_a_registered_user(step):
    assert False, 'This step must be implemented'

@step(u'And no previous reminder was sent in the last 2 weeks')
def and_no_previous_reminder_was_sent_in_the_last_2_weeks(step):
    assert False, 'This step must be implemented'

@step(u'When I wait for 2 weeks')
def when_i_wait_for_2_weeks(step):
    assert False, 'This step must be implemented'

@step(u'Then I receive an email reminder with the most liked events')
def then_i_receive_an_email_reminder_with_the_most_liked_events(step):
    assert False, 'This step must be implemented'

@step(u'Then I receive an email reminder with the most booked events')
def then_i_receive_an_email_reminder_with_the_most_booked_events(step):
    assert False, 'This step must be implemented'

@step(u'And I have a booked non-expired event')
def and_i_have_a_booked_non_expired_event(step):
    assert False, 'This step must be implemented'

@step(u'And no previous reminder was sent')
def and_no_previous_reminder_was_sent(step):
    assert False, 'This step must be implemented'

@step(u'When there are 3 days left till the event date')
def when_there_are_3_days_left_till_the_event_date(step):
    assert False, 'This step must be implemented'

@step(u'Then I receive an email reminder')
def then_i_receive_an_email_reminder(step):
    assert False, 'This step must be implemented'

@step(u'And I have an expired event')
def and_i_have_an_expired_event(step):
    assert False, 'This step must be implemented'

@step(u'And no previous feedback reminder was sent')
def and_no_previous_feedback_reminder_was_sent(step):
    assert False, 'This step must be implemented'

@step(u'When 1 day has passed after event')
def when_1_day_has_passed_after_event(step):
    assert False, 'This step must be implemented'

@step(u'Then I receive a feedback email reminder')
def then_i_receive_a_feedback_email_reminder(step):
    assert False, 'This step must be implemented'

@step(u'And I press the un-subscribe button')
def and_i_press_the_un_subscribe_button(step):
    assert False, 'This step must be implemented'

@step(u'Then I add the user to the un-subscribers list')
def then_i_add_the_user_to_the_un_subscribers_list(step):
    assert False, 'This step must be implemented'

@step(u'And I redirect him to the homepage')
def and_i_redirect_him_to_the_homepage(step):
    assert False, 'This step must be implemented'

@step(u'And I check some checkboxes')
def and_i_check_some_checkboxes(step):
    assert False, 'This step must be implemented'

@step(u'Then I add the user to the un-subscribers list with the specified options')
def then_i_add_the_user_to_the_un_subscribers_list_with_the_specified_options(step):
    assert False, 'This step must be implemented'